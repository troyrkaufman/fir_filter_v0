# FIR Filter Notes

Fir filter works as intended. It is synthesizable but will not close timing. A shift register was used to start producing outputs to align with Xilinx's FIR Compiler. 

Also some of the internal variable names have not been fixed. 